# GitHub repository setup

Suggested repository name: `webmin-wireguard`

Suggested description:

> Secure WireGuard interface and peer management module for Webmin with QR export, traffic history, diagnostics and ACL-aware runtime control.

Suggested topics:

`webmin`, `wireguard`, `vpn`, `perl`, `systemd`, `linux`, `networking`, `wg-quick`

Recommended settings:

- default branch: `main`;
- enable Issues and private vulnerability reporting;
- require the `CI / test` check before merging;
- disallow force pushes to `main`;
- enable Dependabot for GitHub Actions;
- publish tag `v1.0.0` as the first stable release.
