conf_dir=WireGuard configuration directory,0
backup_dir=Backup directory used before configuration changes,0
wg_cmd=Full path to wg (collector restart required),0
wg_quick_cmd=Full path to wg-quick,0
ip_cmd=Full path to ip,0
ping_cmd=Full path to ping,0
traceroute_cmd=Full path to traceroute,0
nc_cmd=Full path to nc,0
stdbuf_cmd=Full path to stdbuf for live diagnostic output,0
journalctl_cmd=Full path to journalctl,0
qrencode_cmd=Full path to qrencode,0
apt_get_cmd=Full path to apt-get for installing optional dependencies,0
dpkg_query_cmd=Full path to dpkg-query for checking installed packages,0
command_timeout=Command timeout in seconds,0
recent_handshake_seconds=Recent handshake threshold in seconds,0
stats_enabled=Enable live traffic graphs?,1,1-Yes,0-No
stats_refresh_interval=Graph refresh interval in seconds (minimum 2) (collector restart required),0
stats_live_window=Live graph window in seconds,0
stats_history_enabled=Collect traffic history in background? (collector restart required),1,1-Yes,0-No
stats_history_retention=Traffic history retention in seconds (collector restart required),0
journal_lines=Default journal line count,0
runtime_timeout=Asynchronous WireGuard runtime request timeout in seconds (collector restart required),0
diagnostic_dir=Directory for asynchronous diagnostic jobs,0
diagnostic_retention=Completed diagnostic job retention in seconds,0
diagnostic_idle_timeout=Continuous diagnostic idle timeout after browser disconnect in seconds,0
diagnostic_max_output=Maximum diagnostic output size in bytes,0
state_dir=Module runtime state directory,0
max_config_bytes=Maximum WireGuard configuration file size in bytes,0
backup_retention=Maximum number of backups retained per configuration,0
stats_max_points=Maximum history points retained per interface or peer (collector restart required),0
stats_max_file_bytes=Maximum bytes per history file (collector restart required),0
stats_max_total_bytes=Maximum total bytes for all metric history files (collector restart required),0
stats_health_interval=Collector health snapshot interval in seconds (collector restart required),0
diagnostic_max_jobs_per_user=Maximum concurrent diagnostic jobs per user,0
systemctl_cmd=Full path to systemctl for collector control,0
